// grumpy =(
const orderSummaryBuilding = state => {
    return state.summary.building
}

const orderSummaryOrder = state => {
    return state.summary.order
}

export default {
    orderSummaryBuilding,
    orderSummaryOrder
}